---
description: Initialize a specification using the canonical SDD workflow
allowed-tools: Bash, Read, Write, Edit, Glob, Grep
argument-hint: [--from-discovery <discovery>] <project-description>
---

Read and execute `.sdd/settings/workflows/_common.md`, then `.sdd/settings/workflows/spec-init.md`. `.sdd/` is canonical; this file is only a Claude adapter. Pass `$ARGUMENTS` as the project description.
