---
description: Generate implementation tasks using the canonical SDD workflow
allowed-tools: Bash, Read, Write, Edit, Glob, Grep
argument-hint: <feature-name> [-y] [--sequential]
---

Read and execute `.sdd/settings/workflows/_common.md`, then `.sdd/settings/workflows/spec-tasks.md`. Load the rules/templates referenced there. `.sdd/` is canonical; this file is only a Claude adapter. Arguments: `$ARGUMENTS`.
